# Dockerfiles

A collection of Dockerfiles for various frameworks and tools, helping simplify and streamline local development workflows.

Each folder operates independently, providing a ready-to-use Dockerfile along with instructions for building and running applications in isolated environments.

---

#### Build Image from Folder

```sh
sudo docker build --no-cache FOLDER_NAME -t IMAGE_NAME
```

---